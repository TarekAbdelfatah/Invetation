# نظام "ابتكار" — وثيقة المهام الفنية (SRS Tasks)
## التقنية: ASP.NET Core MVC (.NET 10 LTS) — EF Core Code First — PostgreSQL (تطوير) → SQL Server (إنتاج)

---

## 1) قرارات معتمدة (حل التضاربات)

- **اللجنة = الإدارة المختصة:** لجنة الابتكار تستخدم نفس نموذج المعايير الخمسة (1–5) تماماً كالإدارة المختصة، مع ذات القوائم المنسدلة وآلية الحساب.
- **المعدل العام المعتمد للقرار:**
  - نسبة الإدارة المختصة = (مجموع نقاطها ÷ 25) × 100
  - نسبة اللجنة = (مجموع نقاطها ÷ 25) × 100
  - **المعدل العام = (نسبة الإدارة + نسبة اللجنة) ÷ 2**
  - (تُجبر الكسور العشرية لأقرب رقم صحيح)
- **تم حذف "فهرس البيانات" نهائياً** والاعتماد على **جدول المعايير الخمسة المركزي فقط** كمصدر وحيد للتقييم في جميع المراحل.
- **الإدارة المختصة** = الجهة المنفذة تقنياً للمقترح (مثال: فكرة تخص الذكاء الاصطناعي توجَّه للإدارة التقنية).
- **الإدارة الشريكة** = جهة مكملة ذات اختصاص متقاطع (مثال: البنية التحتية) ويُطلب رأيها "الاسترشادي" فقط.

---

## 2) التقنيات المعتمدة

| المكوّن | الاختيار |
|--------|----------|
| Framework | ASP.NET Core MVC — .NET 10 LTS |
| ORM | EF Core 10 — Code First + Migrations (Npgsql PostgreSQL للتطوير / SqlServer للإنتاج) |
| Database | PostgreSQL (Code First — للتطوير حالياً) → SQL Server (الإنتاج) — التبديل عبر Provider + ConnectionString |
| المصادقة | مصادقة مخصصة بسيطة (Username/Password + PBKDF2) + Cookie للجلسة + الأدوار من جدول UserRole — بدون ASP.NET Core Identity |
| تكامل الخارجي | (مؤجل) Nafath / SSO بوابة الديوان — سيُربط لاحقاً في الإنتاج؛ حالياً الدخول موحّد بالاسم وكلمة المرور |
| الواجهة | Razor Views + Server-Side Rendering أولاً + Bootstrap 5 RTL (**CSS فقط** — بدون Bootstrap JS/jQuery) + الحد الأدنى من Vanilla JS مكتوب يدوياً |
| الهوية البصرية | `BOG-visual-identity.md` (دليل الألوان/الخطوط/المكوّنات) + `bog-theme.css` (متغيرات CSS) — خط IBM Plex Sans Arabic + أيقونات Material Icons |
| الملفات | مجلد مشترك على الخادم مع تخزين المسار في قاعدة البيانات |
| التقارير | مؤجلة — تُحدد مكتبة التصدير في مهمة لاحقة |

---

## 3) الأدوار (مصفوفة صلاحيات مركزية)

| الدور | الصلاحيات |
|------|-----------|
| مستفيد خارجي | تقديم + متابعة/تعديل طلباته فقط |
| موظف داخلي | تقديم + متابعة طلباته (الاسم والإدارة آلياً) |
| موظف تدقيق | وارد (جديد/معاد) + قبول/رفض/استكمال نواقص |
| إدارة مختصة | تقييم 5 معايير + توجيه (لجنة/شريكة) + تنفيذ |
| إدارة شريكة | مرئيات استرشادية فقط + إعادة لعدم الاختصاص |
| لجنة الابتكار | تقييم 5 معايير + قرارات + تصويت + تفويض |
| مدير النظام | عرض شامل قراءة فقط + تقارير + إدارة لجان |

**ملاحظة:** كل الأدوار تدخل عبر نفس شاشة الدخول الموحدة (اسم مستخدم + كلمة مرور).

**ملاحظة:** "رئيس اللجنة" يُحدد في مصفوفة الصلاحيات، وليس من نموذج تشكيل اللجنة.

---

## 4) الحالات (تعريف مركزي — 10 حالات)

| # | الحالة | عند من / السبب |
|---|--------|----------------|
| 1 | جديد | إرسال المستفيد الأولى (لم يفتحه موظف التدقيق بعد) |
| 2 | تحت الدراسة | من لحظة فتح موظف التدقيق للطلب، وتمتد عبر كامل مسار المراجعة (مراجعة التدقيق ← التوجيه للإدارة المختصة ← تقييم الإدارة ← مرئيات الإدارات الشريكة ← تقييم اللجنة) حتى يصدر قرار من اللجنة الابتكارية |
| 3 | بانتظار استكمال نواقص | طلب التدقيق استكمال بيانات — عودة مؤقتة خارج مسار الدراسة |
| 4 | معاد إرساله | إعادة المستفيد الإرسال (بعد الاستكمال أو التطوير) — بانتظار استئناف الدراسة |
| 5 | فكرة معادة للتطوير | قرار اللجنة (61–79%) — إعادة الفكرة للمبتكر |
| 6 | مرفوض (تدقيق) | رفض التدقيق المرحلة الأولى — نهائي |
| 7 | رفض فني | قرار اللجنة (<60%) — نهائي |
| 8 | قبول الفكرة | قرار اللجنة (≥80%) + إحالة للإدارة التنفيذية |
| 9 | مغلق | انتهاء مهلة 14 يوم دون رد |
| 10 | مكتملة التنفيذ | الإدارة التنفيذية تحدد مرحلة "تم التنفيذ" |

> **ملاحظة معتمدة:** أُلغيت حالة "قيد التقييم الفني" نهائياً. أصبحت **"تحت الدراسة"** حالة مظلية واحدة تغطي الطلب من لحظة فتحه من قبل موظف التدقيق وحتى اتخاذ اللجنة الابتكارية القرار عليه، وتتضمن داخلياً جميع مراحل (التدقيق، التقييم الفني، مرئيات الشركاء، مراجعة اللجنة). الحالات (بانتظار استكمال نواقص / معاد إرساله / فكرة معادة للتطوير) هي حلقات عودة مؤقتة تعيد الطلب لمساره ثم تعود حالته إلى "تحت الدراسة" عند استئناف المعالجة.

---

## 5) المعايير الخمسة (جدول ثوابت مركزي — المصدر الوحيد)

| المعيار | القائمة المنسدلة | النسبة الموازية |
|---------|----------------|-----------------|
| المواءمة الاستراتيجية | 1–5 | 20% / 40% / 60% / 80% / 100% |
| حجم الأثر | 1–5 | 20% / 40% / 60% / 80% / 100% |
| قابلية التنفيذ | 1–5 | 20% / 40% / 60% / 80% / 100% |
| مستوى الابتكار | 1–5 | 20% / 40% / 60% / 80% / 100% |
| توظيف التقنيات الحديثة | 1–5 | 20% / 40% / 60% / 80% / 100% |

**القواعد:**
- مجموع النقاط: حد أدنى 5 / أقصى 25.
- معدل الطرف (من 5) = مجموع نقاطه ÷ 5.
- نسبة الطرف (%) = (مجموع نقاطه ÷ 25) × 100.
- أسماء المعايير تُجلب من جدول الثوابت ولا يمكن تعديلها من الواجهة.

---

## 6) الكيانات (Code First)

### 6.1 كيانات الهوية والصلاحيات
- `User` (جدول `Users` — بدون وراثة Identity): Id، Username (فريد)، PasswordHash (PBKDF2)، FullName، Email، UserType (External/Internal)، DepartmentId?، IsActive، CreatedAt، LastLoginAt
- `Role`: Id، Name، NameEn، Code، IsActive
- `UserRole` (ربط Many-to-Many بين User و Role)
- `Department` (الإدارات: 'التقنية'، 'البنية التحتية'...) — حقول: Id، Name، NameEn، IsActive، Code
- `Committee` + `CommitteeMember` (عضو ↔ لجنة ↔ مستخدم، IsHead)
- `Delegation` (تفويض رئيس اللجنة): DelegatedFromId، DelegatedToId، StartAt، EndAt، Reason، IsActive

### 6.2 كيان الطلب (InnovationIdea) — نموذج مشترك واحد
حقول أساسية:
- `ReferenceNumber` (رقم مرجعي — صيغة تُحدد: مثال `INV-000001`)
- `Title`, `Summary (≤3000)`, `CurrentChallenges (≤3000)`, `ProposedSolution (≤300)`, `RequiredResources`
- `ExpectedImpact` (Dropdown: مالي / على الجهة / على المستفيد / أخرى) + `ExpectedImpactOther`
- `TargetAudience` (Dropdown) + `TargetAudienceOther`
- `InnovationDomain` (Dropdown: خدمات قضائية / إجراءات إدارية / تحول رقمي)
- `UsesEmergingTech` (bool) + `EmergingTechs` (List<Technology>) + `TechnologyOthers`
- `ApplicantType` (Internal/External), `ApplicantId`, `ApplicantDepartmentId` (للداخلي)
- `CurrentStatus`, `CreatedAt`, `SubmittedAt`
- `AuditEmployeeId` (موظف التدقيق الحالي/الأخير)
- `AssignedDepartmentId` (الإدارة المختصة الحالية)
- `ClosedAt`, `IsClosed`

### 6.3 كيانات الحالة والمسار
- `IdeaStatusHistory`: IdeaId، FromStatus، ToStatus، ActionBy، DateTime، Comment (سجل تسلسلي لا يُعدل)
- `AuditLog`: UserId، Action، EntityType، EntityId، DateTime، IP، Data(snapshot)
- `AuditActionItem`: ترتبط بكل إجراء من التدقيق (قبول/رفض/استكمال) — Decision، DecisionText، TargetDepartmentId، AuditDate
- `Attachment`: IdeaId، FileName، StoredPath، SizeBytes، FileType، UploadedBy، UploadedAt، IsMandatoryRequired
- `DevelopmentFeedback` (نقاط تطوير لجنة): IdeaId، Points، CreatedBy، CreatedAt
- `RejectionRecord`: IdeaId، Stage (تدقيق/لجنة)، Reason، CreatedBy، CreatedAt

### 6.4 كيانات التقييم (المركزي للمعايير الخمسة)
- `AssessmentCriterion` (الجدول المركزي): Code، Name، NameEn، Order، IsActive
- `CriterionScoring`: id، Score (1–5)، Percent (20..100)
- `AssessmentHeader`: IdeaId، Stage (SpecializedDept/PartnerDept/Committee)، AssessorId، TotalScore، Percent، Average5، Status (Draft/Submitted/ReadOnly)
- `AssessmentDetail`: AssessmentHeaderId، CriterionId، Score
- `PartnerAssignment`: IdeaId، PartnerDepartmentId، SentAt، Status (Pending/InReview/Submitted/Late/Returned)، ReturnReason، LateNote

### 6.5 كيانات التصويت والتنفيذ
- `Vote`: IdeaId، MemberId، Choice (Agree/Disagree/NeedsDev)، CreatedAt، UpdatedAt، SubmittedAt، IsSubmitted
- `ExecutionUpdate`: IdeaId، Stage (خمس مراحل)، Description، Docs، CreatedByDept، CreatedByUser، CreatedAt
- `Notification`: ToUserId، Types، Message، ReferenceType/Id، ReadAt
- `IdeaTimelineEntry` (السجل الزمني التراكمي): IdeaId، EntryType، Title، Description، Meta

---

## 7) المهام الفنية (Tasks) حسب Epic

> **تسمية الملفات:** كل مهمة تذكر الملفات المتوقع إنشاؤها (`.cs`, `.cshtml`, `.js`, `.css`).
> **البدء الموصى به:** Epic 0 ثم Epic 1 → Epic 2 → ... بالترتيب.
> **بنية المشروع:** مشروع واحد (بدون مشاريع متعددة) — الطبقات كفولدات: `Models/` (الكيانات)، `DTOs/`، `Repositories/`، `Services/`، `Data/` (DbContext + Migrations).
> **مكوّنات مشتركة:** أي عنصر يتكرر في أكثر من صفحة/دور (هيدر، فوتر، بادجات الحالة، مودالات التأكيد، التنبيهات، القوائم) يُبنى كـ **Partial View** (`_*.cshtml` في `Views/Shared`) ويُعاد استخدامه — ممنوع تكرار الـ markup.
> **موبايل-فيرست:** كل صفحة تظهر للمستفيد الخارجي تُصمم موبايل-فيرست أولاً.

---

### Epic 0 — تهيئة المشروع (البنية الأساسية)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-001 | مشروع واحد + هيكل الفولدات | مشروع MVC واحد `Ibtikar` (بدون تقسيم إلى مشاريع متعددة) — الطبقات كفولدات: `Models/`، `DTOs/`، `Repositories/`، `Services/`، `Data/` (DbContext + Migrations)، `Controllers/`، `Views/`. تمكين RTL والثقافة `ar-SA` في `Program.cs` | عالية |
| T-002 | إعداد DbContext واتصال DB | حزم: `Microsoft.EntityFrameworkCore` + `Npgsql.EntityFrameworkCore.PostgreSQL` + `Microsoft.EntityFrameworkCore.Tools`. كونيكشن سترينج Npgsql في `ConnectionStrings:DefaultConnection` (مثال: `Host=...;Port=5432;Database=...;Username=...;Password=...;SSL Mode=Require;Trust Server Certificate=true`) — الأسرار في `appsettings.Secrets.json` (مستبعد من git). `AddDbContext` + جدول `Users` | عالية |
| T-003 | الكيانات الأساسية الأولى | ملفات `Department.cs`, `User.cs`, `Role.cs`, `UserRole.cs` + `IEntityTypeConfiguration` لكل كيان | عالية |
| T-004 | أول Migration | `dotnet ef migrations add InitialSchema` + `Database.Migrate()` عند الإقلاع | عالية |
| T-005 | Seed Data | زرع: الأدوار السبعة + **مستخدم اختبار لكل دور** (اسم مستخدم/كلمة مرور موحدة)، جدول الحالات، **جدول المعايير الخمسة**، الإدارات | عالية |
| T-006 | قالب أساسي `_Layout` | ملفات `_Layout.cshtml`, `_ViewImports`, `_ViewStart` — Bootstrap 5 RTL (CSS فقط) + `bog-theme.css` + خط IBM Plex Sans Arabic + Material Icons + نظام RTL وألوان الحالات | عالية |
| T-007 | معالجة أخطاء عامة | `ErrorController` + صفحات 404/500 + `ExceptionMiddleware` يسجل في `AuditLog` | متوسطة |
| T-008 | سجلات مركزية | `AuditLogService` (يسجل كل إجراء: مستخدم، وقت، IP، بيانات قبل/بعد) | عالية |
| T-009 | تفعيل الهوية البصرية | استيراد `bog-theme.css` (في `wwwroot/css/`) + تخصيص متغيرات Bootstrap بألوان BOG (primary `#1B8354`, hover `#166A45`, active `#104631`, الثانوي `#DBA102`) + الرجوع لـ `BOG-visual-identity.md` | عالية |
| T-010 | مكوّنات مشتركة (Partial Views) | `Views/Shared/_Header.cshtml`, `_Footer.cshtml`, `_StatusBadge.cshtml`, `_ConfirmModal.cshtml`, `_Alert.cshtml`, `_Pagination.cshtml` — تُعاد استخدامها في كل الأدوار | عالية |
| T-011 | مكوّنات JavaScript أساسية (Vanilla) | `wwwroot/js/site.js` مكتوب يدوياً بأبسط صورة: modal تأكيد الحذف، toast/تنبيه، إظهار/إخفاء حقل مرتبط، عدّاد أحرف — بدون jQuery أو أي مكتبة | عالية |

---

### Epic 1 — المصادقة والوصول (دخول موحّد بالاسم وكلمة المرور — بدون Identity / Nafath / SSO)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-101 | شاشة تسجيل الدخول الموحدة | `Views/Account/Login.cshtml` — اسم مستخدم + كلمة مرور + زر دخول + رسائل خطأ عربية (بدون تسجيل ذاتي وبدون استعادة كلمة مرور) | عالية |
| T-102 | خدمة المصادقة المخصصة | `AuthService.LoginAsync` — بحث في جدول `Users` بالاسم + تحقق `PasswordHash` (PBKDF2) + `SignInAsync` (Cookie) + تحديث `LastLoginAt` | عالية |
| T-103 | تسجيل الخروج وإدارة الجلسة | `Account/Logout` + `Session` timeout (ضبط في `Program.cs`) + إعادة توجيه لصفحة الدخول عند انتهاء الجلسة | عالية |
| T-104 | تحميل الأدوار في الجلسة | قراءة أدوار المستخدم من `UserRole` وتخزينها في `Claims` عند الدخول + `[Authorize(Roles=...)]` على الكونترولرات | عالية |
| T-105 | توجيه حسب الصلاحية | `HomeController` يعيد توجيه كل دور إلى لوحته في `Home/Index` | عالية |

---

### Epic 2 — نموذج تقديم الفكرة (مشترك داخلي/خارجي)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-201 | ViewModel النموذج | `IdeaCreateViewModel` بجميع الحقول + `DataAnnotations` (Required, MaxLength) بالعربية | عالية |
| T-202 | كيان `InnovationIdea` + Migration | كامل الحقول الموثقة في §6.2 | عالية |
| T-203 | شاشة النموذج | `Views/Ideas/Create.cshtml` — زر "حفظ" و"إرسال" + عدادات تنازلية في `idea-form.js` | عالية |
| T-204 | عداد التنازلي (JS خالص) | فونتJS يحسب الأحرف المتبقية (3000/3000/300) ويمنع تجاوز الحد | عالية |
| T-205 | حقل "أخرى" للأثر المتوقع (JS) | إظهار فوري لحقل نصي عند اختيار "أخرى" مع إلزاميته | عالية |
| T-206 | حقل "أخرى" للفئة المستهدفة (JS) | إظهار فوري لحقل نصي عند "أخرى" | عالية |
| T-207 | الإظهار/الإخفاء للتقنيات (JS) | قائمة التقنيات تظهر فقط عند "نعم" + حقل "أخرى" عند اختيارها | عالية |
| T-208 | رفع الملفات — الواجهة | `attachment-upload.js` — سحب/اختيار، حد ملفين، شريط تقدم `XMLHttpRequest` | عالية |
| T-209 | رفع الملفات — الخادم | `AttachmentService.UploadAsync` → المجلد المشترك + تسجيل مسار + حجم + تحقق امتداد | عالية |
| T-210 | التحقق من PDF الحقيقي (Signature check) | قراءة أول 4 بايتات `%PDF` في `PdfValidator` + رفض الملفات المزيفة | عالية |
| T-211 | التحقق من الحجم | رفض >5MB فوراً برسالة "تجاوزت الحد المسموح به (5MB)" | عالية |
| T-212 | حفظ مسودة | `Ideas/SaveDraft` → بيانات `Draft` + إمكانية الاسترجاع | منخفضة |
| T-213 | إرسال الطلب | `Ideas/Submit` → تحقق إلزاميات + توليد `ReferenceNumber` + حالة "جديد" + `StatusHistory` | عالية |
| T-214 | رسالة نجاح + رقم مرجعي | `TempData`/عرض نجاح مع الرقم المرجعي + توجيه | عالية |
| T-215 | التكامل مع نظام الإجراءات الداخلي | `ProcedureGatewayService` (HTTP call) بعد الإرسال مع Retry لمرة | عالية |
| T-216 | معالجة انقطاع الاتصال | `idea-form.js`: حفظ تلقائي في `localStorage` + كشف `navigator.onLine` + رسالة فشل بدون فقد البيانات | عالية |
| T-217 | جلب بيانات الموظف الداخلي | تعبئة `ApplicantName/Department` من الجلسة تلقائياً + إخفاء الإدخال اليدوي | عالية |

**معايير القبول (النموذج):** كل الحقول الإلزامية تعبأ قبل الإرسال · "أخرى" يظهر فوراً · التقنيات تظهر فقط لـ"نعم" · خطأ أحمر "يرجى توضيح الأثر المتوقع" عند ترك "أخرى" فارغة · ملف 5.1MB يُوقف فوراً.

---

### Epic 3 — لوحة المستفيد ومتابعة الطلبات

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-301 | جدول "طلباتي السابقة" | `Views/MyRequests/Index.cshtml` — أعمدة: رقم، عنوان، تاريخ، حالة، شريط تقدم؛ ترتيب حديث←قديم | عالية |
| T-302 | فلترة المالك | تقييد الاستعلام بـ `WHERE ApplicantId = @currentUser` (Row-Level) | عالية |
| T-303 | مؤشرات الحالة | `status-badges.css`/`status-badges.js` — لون/أيقونة لكل حالة | عالية |
| T-304 | شريط تقدم بصري | `progress-bar.js` يحدد مرحلة الفكرة (تقديم→تدقيق→تقييم→لجنة→تنفيذ) | متوسطة |
| T-305 | صفحة ترحيبية فارغة | عند لا طلبات: رسالة "لم تقم بتقديم أي أفكار حتى الآن" + زر "ابتكر الآن" | منخفضة |
| T-306 | عدم إظهار بيانات الموظفين | منع عرض أي أسماء/تواريخ إجراءات داخلية في جدول أو تفاصيل المستفيد | عالية |
| T-307 | شاشة التفاصيل حسب الحالة | `Views/MyRequests/Details.cshtml` — فتح/قفل الحقول وقراءة/تعديل حسب حالة الطلب | عالية |
| T-308 | صندوق ملاحظات التدقيق | `audit-notes-box` — برتقالي واضح أعلى الاستمارة (حالة استكمال) | عالية |
| T-309 | زر إعادة إرسال النواقص | تفعيل فقط بعد تعديل ≥1 + تعطيل فوري + `loading` لمنع الازدواجية | عالية |
| T-310 | زر إعادة إرسال الفكرة المطورة | يفتح النموذج كاملاً + نقاط التطوير أعلى + تعطيل بعد الضغط | عالية |
| T-311 | منع حذف مرفق إلزامي | تحقق قبل الحفظ/الإرسال + رسالة حمراء "يرجى رفع الملفات الأساسية لإتمام العملية" | عالية |
| T-312 | مؤقت 14 يوم | `BackgroundService` يفحص يومياً ويحوّل "قيد الاستكمال/التطوير" غير المستجاب إلى "مغلق" + إخفاء زر التعديل | عالية |
| T-313 | رسالة انتهاء المهلة | "انتهت المهلة المتاحة للتعديل، يرجى تقديم طلب جديد" + قفل | عالية |
| T-314 | حالة "مرفوض" | حقول ReadOnly + رسالة حمراء بمبررات الرفض فقط | عالية |
| T-315 | حالة "جديد" | كل الحقول ReadOnly + زر حذف الطلب (للمستفيد) | عالية |

---

### Epic 4 — موظف التدقيق

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-401 | صندوق الوارد | `Views/Audit/Inbox.cshtml` — جدول فقط بحالات: "جديد" و"معاد إرساله" | عالية |
| T-402 | أعمدة الوارد | رقم الطلب، اسم المستفيد، عنوان الفكرة، تاريخ التقديم، الحالة | عالية |
| T-403 | بحث سريع | `inbox-search.js` بحث لحظي في (رقم، مستفيد، عنوان) | عالية |
| T-404 | فلاتر | نوع الطلب (داخلي/خارجي) + حالة (جديد/معاد/مرفوض) | عالية |
| T-405 | تحديث لحظي | `SignalR` أو `Polling (5s)` لوصول طلب جديد دون إعادة تحميل | عالية |
| T-406 | تمييز المتأخر >48 ساعة | `badge` أحمر بجانب الطلبات >48 ساعة بدون إجراء | متوسطة |
| T-407 | رسالة فارغة | "لا توجد طلبات جديدة بانتظار التدقيق حالياً" | منخفضة |
| T-408 | شاشة تفاصيل الطلب | `Views/Audit/Details.cshtml` — بيانات كاملة + مرفقات + قسم إجراء. **عند فتح موظف التدقيق للطلب تتحول الحالة من "جديد" إلى "تحت الدراسة"** وتسجل في `IdeaStatusHistory` | عالية |
| T-409 | إجراء "قبول وتوجيه" | قائمة إدارات نشطة فقط + اختيار إدارة واحدة + "اعتماد التوجيه" — **حالة الطلب تبقى "تحت الدراسة"** ويرتبط الطلب بالإدارة المختصة المختارة (`AssignedDepartmentId`) | عالية |
| T-410 | رفض الإدارات غير النشطة | رسالة "يرجى اختيار إدارة نشطة" | عالية |
| T-411 | إجراء "رفض الطلب" | حقل أسباب إلزامي + "تأكيد الرفض" → حالة "مرفوض(تدقيق)" نهائية | عالية |
| T-412 | إجراء "طلب استكمال نواقص" | حقل تعليمات إلزامي → حالة "بانتظار استكمال نواقص" | عالية |
| T-413 | إشعار نجاح للموظف | بعد قبول/توجيه/رفض | عالية |
| T-414 | إشعار مدير الإدارة (قبول) | بريد + إشعار داخلي لمدير الإدارة المختصة | عالية |
| T-415 | إشعار المستفيد (رفض/استكمال) | بريد بمبررات الرفض أو تعليمات الاستكمال | عالية |
| T-416 | العودة لنفس الموظف | عند "معاد إرساله" يبقى `AuditEmployeeId` الأصلي | متوسطة |
| T-417 | تسجيل `AuditActionItem` + `AuditLog` | أي إجراء يكتب Decision/DecisionText/TargetDepartmentId/DateTime | عالية |

---

### Epic 5 — الإدارة المختصة (المنفذ التقني الأساسي)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-501 | لوحة معلومات الإدارة المختصة | `Views/SpecializedDashboard/Index.cshtml` + بطاقات KPI لحظية | عالية |
| T-502 | بطاقة 1: أفكار قيد الدراسة | عدّ الحالات المحالة للإدارة دون إجراء | عالية |
| T-503 | بطاقة 2: أفكار معروضة للشريكة | عدّ الافكار بانتظار مرئيات شريكة | عالية |
| T-504 | بطاقة 3: محالة للتنفيذ | عدّ القرارات النهائية المقبولة | عالية |
| T-505 | بطاقة 4: مرفوضة | عدّ المرفوضات في كل المراحل (باستثناء تدقيق) | عالية |
| T-506 | فلاتر (حالة تقديم + نوع طلب) | `dashboard-filters.js` | عالية |
| T-507 | جدول الإحالات | رقم، عنوان، تاريخ إحالة، موظف تدقيق، Badge نوع (داخلي أزرق/خارجي أخضر)، اسم الإدارة للداخلي، **مدة البقاء** (على مدار الساعة) | عالية |
| T-508 | مدة بقاء الفكرة | `StayDurationTimer` — بدء العد من لحظة الإحالة | عالية |
| T-509 | عرض تفاصيل الفكرة الكاملة (قراءة فقط) | كل البيانات + كل التقييمات + Timeline، بدون إدخال | عالية |
| T-510 | نموذج تقييم المعايير الخمسة | `Views/SpecializedDashboard/Assess.cshtml` — قوائم 1–5 لكل معيار + وصف المعيار | عالية |
| T-511 | حساب تلقائي (الإدارة) | JS لحظي: مجموع نقاط، معدل من 5، نسبة % | عالية |
| T-512 | منع إرسال بتقييم ناقص | تمييز المعيار الأحمر + "يرجى استكمال تقييم كافة المعايير قبل اتخاذ إجراء الإرسال" | عالية |
| T-513 | حفظ التقييم كمسودة | `AssessmentHeader.Status=Draft` | متوسطة |
| T-514 | زر "إرسال مباشرة إلى لجنة الابتكار" | تأكيد "هل أنت متأكد..." + اعتماد + نقل لسلة اللجنة + تقييم يصبح ReadOnly | عالية |
| T-515 | زر "طلب مرئيات الإدارة الشريكة" | قائمة إدارات (تستثني الحالية + لا شريكة موجودة) + إلزام تحديد واحدة + ملاحظات اختيارية | عالية |
| T-516 | منع الإحالة الذاتية | استعلام يستبعد `AssignedDepartmentId` من القائمة | عالية |
| T-517 | إنشاء `PartnerAssignment` | الحفاظ على حالة Pending بعد الإرسال للشريكة | عالية |

#### US-5.3 متابعة الإدارات الشريكة

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-518 | جدول متابعة الشريكة | `Views/SpecializedDashboard/FollowUp.cshtml` — اسم الإدارة، تاريخ الإحالة، الحالة، تاريخ الرد، زر "عرض المرئيات" | عالية |
| T-519 | حالات الشريكة | Pending / InReview / Submitted / Late(بعد 4 أيام) — الأحمر للمتأخر | عالية |
| T-520 | عرض مرئيات الشريكة | تقييم استرشادي + ملاحظات نصية في نافذة/قسم | عالية |
| T-521 | تعديل تقييم الإدارة بعد المرئيات | إعادة تفعيل الإدخال قبل الرفع النهائي + حفظ التعديلات | عالية |
| T-522 | حقل ملاحظة عند تأخر الشريكة | حقل نصي بجانب اسم الإدارة المتأخرة يُحفظ في `PartnerAssignment.LateNote` وسجل الفكرة | متوسطة |
| T-523 | زر "اعتماد وإرسال إلى اللجنة" | تجميع (تقييم الإدارة + مرئيات الشريكة) في ملف واحد + إرسال | عالية |
| T-524 | تحذير عند إدارات لم ترد | نافذة: "تنبيه: هناك (N) إدارات شريكة لم تقم بإبداء مرئياتها بعد..." بخيارين (إلغاء/تجاوز) | عالية |
| T-525 | تسجيل في سجل الفكرة | لكل توجيه/إرسال: اسم المقيم والإدارة + التاريخ + نوع القرار | عالية |

---

### Epic 6 — الإدارة الشريكة (مثال: البنية التحتية)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-601 | لوحة معلومات الشريكة | بطاقات: قيد الدراسة / منجزة (ردت بالمرئيات) / محالة للتنفيذ | عالية |
| T-602 | أقسام تدفقية متسلسلة | عرض أقسام الفكرة عمودياً: أساسي (قراءة فقط) ← تقييم الإدارة (قراءة فقط) ← تقييم الشريكة (قابل للإنشاء/التعديل حتى الإرسال) | عالية |
| T-603 | منع تعبئة الأقسام السفلية | تعطيل حتى اكتمال السابق | عالية |
| T-604 | نموذج تقييم استرشادي | نفس المعايير الخمسة 1–5 | عالية |
| T-605 | حقل مرئيات إلزامي | `TextArea` متعدد الأسطر ملزم | عالية |
| T-606 | زر "إعادة إرسال للإدارة المختصة" | التحقق من الإلزاميات + تحديث حالة الشريكة إلى Submitted + موافقة | عالية |
| T-607 | مستقلية الردود | كل شريكة تعمل على نسختها دون حجب أخرى | عالية |
| T-608 | سجل منفصل لكل شريكة | تبويب: اسم الشريكة، المقيم، تاريخ الإرسال، الدرجات، نص المرئيات | عالية |
| T-609 | تقييم الإدارة المختصة ReadOnly للشريكة | عرض بدون تعديل | عالية |
| T-610 | منع تعديل الفكرة الأصلية/المرفقات | قفل كامل | عالية |
| T-611 | زر "إعادة لعدم الاختصاص" | يظهر خلال 3 أيام عمل فقط من الإحالة (تحقق زمني) | عالية |
| T-612 | نموذج سبب عدم الاختصاص (إلزامي) | منع الإرسال بدون سبب + إلغاء إلزامية التقييم عند الإعادة | عالية |
| T-613 | وسم أحمر [معاد للإدارة المختصة — خطأ في التوجيه] | + نص المبررات في قسم الشريكة | عالية |
| T-614 | منع التحويل المباشر لشريكة أخرى | يجب العودة للإدارة المختصة أولاً | عالية |
| T-615 | إشعار فوري للإدارة المختصة | عند الإعادة مع السبب | عالية |
| T-616 | رسالة تحذير عند ترك المرئيات فارغة | "يرجى كتابة مرئيات وملاحظات الإدارة الشريكة قبل إعادة إرسال الطلب" + تمييز أحمر | عالية |

---

### Epic 7 — لجنة الابتكار (بنفس آلية الإدارة المختصة)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-701 | لوحة معلومات اللجنة | بطاقات: قيد الدراسة / محالة للتنفيذ / مرفوضة (مرحلة اللجنة) + فلاتر + جدول | عالية |
| T-702 | جدول اللجنة | رقم، عنوان، تاريخ إحالة، إجراءات + أعمدة Badge (داخلي/خارجي) واسم الإدارة للداخلي | عالية |
| T-703 | تفاصيل الفكرة التدفقية | تقييم الإدارة (قراءة فقط) ← تقييم الشريكة إن وجد (قراءة فقط) ← **تقييم اللجنة (فارغ للإدخال)** ← الإجراء النهائي | عالية |
| T-704 | نموذج تقييم اللجنة | نموذج "تقييم مشترك": عمود ثابت (الإدارة — ReadOnly) + عمود فارغ (اللجنة) بنفس المعايير الخمسة | عالية |
| T-705 | حساب تلقائي (اللجنة) | مجموع نقاط اللجنة + نسبة اللجنة (%) | عالية |
| T-706 | **المعدل العام** | `(نسبة الإدارة + نسبة اللجنة) ÷ 2` — يُجبر لأقرب صحيح | عالية |
| T-707 | ملخص النتائج | منطقة أسفل النموذج (قراءة فقط): مجموع الإدارة / مجموع اللجنة / المعدل العام — يتحدث لحظياً | عالية |
| T-708 | رسالة توجه تلقائي | >80% "الفكرة مؤهلة للتطبيق" · 61–79% "بحاجة إلى تطوير" · <60% "رفض مسبب للفكرة" | عالية |
| T-709 | أزرار ديناميكية حسب المعدل | ≥80%: (قبول أخضر/رفض أحمر) · 61–79%: (+ بحاجة لتطوير أزرق) · <60%: (رفض/قبول استثنائي) — إظهار/إخفاء | عالية |
| T-710 | منع حفظ بتقييم ناقص | كل معيار إلزامي من اللجنة قبل تفعيل الأزرار | عالية |
| T-711 | إجراء "قبول" | → حالة "قبول الفكرة" + إشعار للمبتكر + إحالة للإدارة التنفيذية (المختصة) | عالية |
| T-712 | تحذير قبول <40% | نافذة تأكيد إضافية | عالية |
| T-713 | إجراء "رفض" | حقل سبب إلزامي → "رفض فني" + إشعار | عالية |
| T-714 | إجراء "بحاجة لتطوير" | يظهر فقط لنطاق 61–79% → فتح نموذج "نقاط التطوير" + "إرسال للمبتكر" → حالة "فكرة معادة للتطوير" + يختفي من وارد اللجنة | عالية |
| T-715 | كسور عشرية | جبر لأقرب رقم صحيح في كل الحسابات | منخفضة |
| T-716 | نماذج التقييم سرية | لا يظهر أي نموذج تقييم للمستفيد الخارجي | عالية |

### Epic 7.4 — التصويت

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-717 | قائمة الأفكار للتصويت | أفلام تحولها رئيس اللجنة أو تلقائياً + إشعار "ضرورة التصويت" | متوسطة |
| T-718 | أزرار التصويت | موافق / غير موافق / بحاجة إلى تطوير + توضيح صوت أخطر | متوسطة |
| T-719 | صوت واحد لكل فكرة | الـ UniqueConstraint `(IdeaId, MemberId)` + تعديل/سحب قبل الإرسال | عالية |
| T-720 | إيقاف التصويت بعد 3 أيام | BackgroundService يقفل التصويت تلقائياً | عالية |

### Epic 7.5 — تفويض رئيس اللجنة

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-721 | إدارة تفويض الصلاحيات | `Views/Committee/Delegations.cshtml` — إضافة/عرض/إلغاء | متوسطة |
| T-722 | تفويض عضو واحد في الفترة | تحقق من تداخل فترات التفويض | عالية |
| T-723 | قائمة أعضاء فقط | استبعاد غير الأعضاء | عالية |
| T-724 | تفعيل/إلغاء تلقائي لحظي | `DelegationService` يتحقق عند كل طلب صالح | عالية |
| T-725 | منع التفويض المتسلسل | العضو المفوض لا يفوض لغيره | عالية |
| T-726 | إشعارات | بريد + داخلي للمفوض ومدير النظام عند التفعيل/الإلغاء | متوسطة |
| T-727 | وسم في السجل | كل إجراء للمفوض يُسجل "تم بواسطة: [الاسم]" | عالية |

---

### Epic 8 — إدارة اللجان (مدير النظام)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-801 | تشكيل لجنة | `Views/Committees/Index.cshtml` + `Create.cshtml` (اسم اللجنة + إضافة أعضاء يتضمن رئيس واحد) | عالية |
| T-802 | إشعار تفعيل اللجنة | عند "تفعيل اللجنة" يُرسل إشعار لكل الأعضاء | عالية |
| T-803 | قيود التشكيل | رئيس واحد فقط + لا تقل عن رئيس وعضو | عالية |
| T-804 | ربط رئيس اللجنة بمصفوفة الصلاحيات | رئيس يُحدد من إعداد الصلاحيات لا من نموذج التشكيل | عالية |

---

### Epic 9 — التنفيذ والمتابعة (الإدارة التنفيذية)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-901 | قائمة المهام التنفيذية | للفكرة المقبولة "قبول الفكرة" المحالة للإدارة (بعد اللجنة) | عالية |
| T-902 | نافذة "تحديث الحالة" | `Views/Execution/Update.cshtml` (Modal): قائمة 5 مراحل + وصف إنجاز | عالية |
| T-903 | المراحل الخمس | مرفوع للاعتماد / التخطيط والتصميم الفني / بناء النموذج الأولي / قيد الاختبار والتجربة / تم التنفيذ | عالية |
| T-904 | إلزام وصف الإنجاز | منع الحفظ + "يرجى إدخال عبارة موجزة تصف الإنجاز الحالي قبل الحفظ" | عالية |
| T-905 | السجل الزمني التراكمي (Timeline) | `Views/Execution/Timeline.cshtml` — أحدث→أقدم، قراءة فقط، لا يُعدل | عالية |
| T-906 | صيغة العرض | `[التاريخ] - تم الانتقال إلى مرحلة [X] - [الإنجاز] - أُضيف بواسطة: [الإدارة] ([المستخدم])` | عالية |
| T-907 | توقيت/مستخدم تلقائي | يُحفظ Server-Side فقط (DateTime.Now + إدارة/مستخدم الجلسة) — لا يُرسل من العميل | عالية |
| T-908 | "تم التنفيذ" — مرفقات إلزامية | فتح إجباري لرفع ملفين PDF كدليل إثبات إلزامي | عالية |
| T-909 | تأكيد شفهي عند "تم التنفيذ" | "هل أنت متأكد من اكتمال تنفيذ الحل الابتكاري بالكامل؟" | منخفضة |
| T-910 | تحويل لـ "مكتملة التنفيذ" + أرشيف | عند الحفظ × (التأكيد + المرفقات) → نقل لـ `ExecutedArchive` | عالية |
| T-911 | إعادة نفس المرحلة (إضافات) | السماح بإضافة تقدم/ملاحظات داخل نفس المرحلة | متوسطة |
| T-912 | عرض Timeline للقراءة فقط | لمدير النظام والإدارة التنفيذية واللجنة | عالية |

---

### Epic 10 — مدير النظام (اللوحة الشاملة)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-1001 | لوحة شاملة | كل البطاقات (قيد الدراسة/معروضة للشريكة/محالة للتنفيذ/مرفوضة) + مدة بقاء + جدول بكل التفاصيل | عالية |
| T-1002 | العرض الشامل للفكرة | قراءة فقط + كل التقييمات + Timeline كامل | عالية |
| T-1003 | منع الإدخال لمدير النظام | قفل كل حقول الإدخال في كل المراحل | عالية |

---

### Epic 11 — تقارير (بدون مكتبة تصدير محجوزة — مؤجلة)

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-1101 | تقرير إحصائيات عام | نطاق (من/إلى) + بطاقات KPI الأربع + نسب مئوية + جدول مراحل (عدد/نسبة/متوسط بقاء). **توزيع المراحل مبني على الحالات المركزية العشر + مؤشرات المراحل الفرعية داخل "تحت الدراسة" من `IdeaStatusHistory`** (مثال: لدى التدقيق، لدى الإدارة المختصة، لدى الشريكة، لدى اللجنة) دون إظهار حالة "قيد التقييم الفني" كحالة مستقلة | عالية |
| T-1102 | معادلة النسبة | `100 × عدد طلبات المرحلة ÷ الإجمالي العام` | عالية |
| T-1103 | منع تاريخ بدء > نهاية | تحقق من `DataAnnotations` + رسالة | عالية |
| T-1104 | تحقق تطابق المجاميع | مجموع الحالات = الإجمالي الفعلي (اختبار حسابي) | عالية |
| T-1105 | رسالة نطاق فارغ | "لا توجد بيانات مسجلة في هذا النطاق الزمني، يرجى تغيير التواريخ" | منخفضة |
| T-1106 | تقرير التحديات والحلول | جدول: رقم الطلب، التحدي، الحل المقترح، تصنيف (Badge) + فلتر مجال ابتكار | عالية |
| T-1107 | استبعاد مرفوضات التدقيق | من تقرير التحديات والحلول تلقائياً | عالية |
| T-1108 | حجز بيانات التقرير | الأرقام لحظية من قاعدة البيانات حتى لحظة التوليد | عالية |
| T-1109 | (مؤجلة) مكوّن التصدير EXCEL/PDF | واجهة `IReportExportService` تُنفذ لاحقاً بمكتبة محددة — الواجهة تُجهّز الآن | متوسطة |

---

### Epic 12 — الأمان وتكامل الأنظمة

| # | المهمة | الوصف والمخرجات | الأولوية |
|---|--------|-----------------|----------|
| T-1201 | CSRF Protection | `AutoValidateAntiforgeryToken` لكل النماذج | عالية |
| T-1202 | Rate Limiting | على إرسال الطلب/المصادقة | عالية |
| T-1203 | التعقيم وXSS | output encoding افتراضي + فلترة إدخال النصوص | عالية |
| T-1204 | SQL Injection | EF Parameterization فقط + حظر `RawSql` غير المراقب | عالية |
| T-1205 | TLS/Encryption | HTTPS إلزامي + تشفير حساسات (مفاتيح اتصالات) | عالية |
| T-1206 | أمان الملفات | حفظ خارج webroot + توليد اسم فريد (GUID) + قراءة عبر endpooint مسؤول بالتحقق | عالية |
| T-1207 | خدمة إشعارات موحدة | `NotificationService.SendAsync` (بريد + بحث داخلي) تُستخدم في كل الأحداث | عالية |
| T-1208 | WebSocket/Polling | `SignalR` لخانات الوارد/Live dashboards | عالية |
| T-1209 | تكامل نظام الإجراءات | `ProcedureGatewayService` (انظر T-215) مع `Timeout/Retry/Log` | عالية |
| T-1210 | Backup خطة | صفحة إعداد/جدولة (أو توثيق) + اختبار استرداد | متوسطة |
| T-1211 | Session/Data Protection | `AddDataProtection` بثبات المفاتيح + Timeout 20 دقيقة | عالية |

---

## 8) سجل الحالات الطرفية الموحدة (مرجع قابل للاختبار)

| الحالة الطرفية | السلوك المطلوب |
|----------------|----------------|
| "أخرى" (أثر/فئة/تقنية) بلا نص والإرسال | منع + تمييز أحمر + رسالة محددة |
| ملف بحجم 5.1MB | إيقاف فوري + "تجاوزت الحد المسموح به (5MB)" |
| انقطاع الإنترنت وقت الإرسال | رسالة فشل + بيانات محفوظة (localStorage) |
| محاولة إعادة إرسال دون تعديل | "عذراً، يجب عليك تحديث البيانات بناءً على الملاحظات قبل إعادة الإرسال" |
| حذف مرفق إلزامي | منع + رسالة حمراء محددة |
| تجاوز مهلة 14 يوم | حالة "مغلق" تلقائياً + إخفاء زر التعديل |
| معيار غير مقيَّم (إدارة/لجنة) | منع الإرسال + تمييز أحمر + رسالة استكمال |
| توجيه لإدارة غير نشطة | رسالة خطأ |
| طلب مرئيات دون اختيار إدارة شريكة | "يرجى تحديد الإدارة الشريكة المطلوبة للمتابعة" |
| تأخر شريكة >4 أيام | حالة Late حمراء + حقل ملاحظة |
| إدارات شريكة لم ترد قبل رفع اللجنة | نافذة تحذير بخيارين (إلغاء / تجاوز) |
| قبول اللجنة لفكرة <40% | تحذير تأكيدي إضافي |
| كسور عشرية في المعدل | جبر لأقرب رقم صحيح |
| تصويت ثانٍ لنفس العضو/الفكرة | رفض (UniqueConstraint) |
| تفويض متداخل الفترات | رفض التفويض الجديد |
| بدء تاريخ بعد الانتهاء في التقرير | منع التوليد |
| نطاق تقرير بلا بيانات | رسالة "لا توجد بيانات" |
| إعادة لعدم الاختصاص بعد 3 أيام عمل | خيار مقفول |

---

## 9) تسلسل التنفيذ الموصى به

```
Epic 0 (البنية) → Epic 1 (المصادقة) → Epic 2 (النموذج) → Epic 3 (طلبات المستفيد)
→ Epic 4 (التدقيق) → Epic 5 (الإدارة المختصة) → Epic 6 (الإدارة الشريكة)
→ Epic 7 (اللجنة) → Epic 8 (إدارة اللجان) → Epic 9 (التنفيذ)
→ Epic 10 (مدير النظام) → Epic 11 (التقارير) → Epic 12 (أمان وتكامل)
```

**مبادئ إلزامية للمطورين:**
- **Server-Side أولاً:** كل المنطق والحسابات والتحقق وإدارة الحالة والبحث والتحديثات تتم على الخادم (Razor + Postback).
  - **JavaScript مسموح فقط في الضرورات** وضمن قائمة قصيرة معلنة: عدّاد الأحرف، إظهار/إخفاء حقل مرتبط باختيار، شريط تقدم رفع الملفات، تأكيد النوافذ.
  - **ممنوع في المتصفح:** أي منطق أعمال أو حسابات أو تحقق، jQuery/مكتبات UI، والكود المعقد — لأن الصيانة تتم من مطور مبتدئ، فأي JavaScript لازم يكون بأبسط صورة ممكنة.
- **Bootstrap للتصميم فقط:** نستخدم Bootstrap 5 RTL (CSS فقط) — ممنوع Bootstrap JS / jQuery / أي مكتبة JavaScript خارجية؛ كل JavaScript يُكتب يدوياً (Vanilla JS) داخل المشروع لتقليل سطح الثغرات وتفادي رفع إصلاحات أمنية متكررة.
- **موبايل-فيرست:** أي صفحة تظهر للمستفيد الخارجي تُصمم موبايل-فيرست أولاً.
- **Partial Views للمشترك:** المكوّنات/الصفحات المتكررة تُبنى كـ `_*.cshtml` في `Views/Shared` وتُعاد استخدامها (لا تكرار للـ markup).
- كل تغيير حالة يُسجل في `IdeaStatusHistory` + `AuditLog`.
- كل الجداول الثابتة (المعايير، الحالات، الإجراءات) من قاعدة البيانات وليست hardcoded.
- كل التواريخ تُخزن UTC + عرض محلي.
- كل المهام قابلة للاختبار المستقل وترتبط بمعايير قبول واضحة.